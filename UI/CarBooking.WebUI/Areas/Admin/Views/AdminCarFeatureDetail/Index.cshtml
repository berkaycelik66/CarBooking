@model List<ResultCarFeatureByCarIdDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Araca Ait Özellikler Listesi</h4>
                        <p class="card-subtitle mb-4">
                            Araca ait özellik eklemesi yapabilir,mevcut özellikleri kaldırabilir, ilgili güncellemeleri gerçekleştirebilirsiniz.
                        </p>
                        <form method="post">
                            @foreach(var item in Model)
                            {
                                <div class="form-check">
                                    <input type="hidden" asp-for="@item.CarFeatureID" />
                                    <input type="hidden" asp-for="@item.FeatureName" />
                                    <input type="checkbox" asp-for="@item.IsPresent" class="form-check-input" />
                                    <label class="form-check-label" for="@item.IsPresent">@item.FeatureName</label>
                                </div>
                            }
                            <br />
                            <button class="btn btn-success">Değişiklikleri Kaydet</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

